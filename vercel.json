{
  "buildCommand": "cd registry-api && npm run build",
  "outputDirectory": "registry-api/.next",
  "installCommand": "cd registry-api && npm install",
  "framework": "nextjs",
  "rootDirectory": "registry-api"
}